#include "dungeon-events.h"

void execute_player_action() {
    // Existing code for player actions that consume a turn

    // Hook up the event director
    trigger_dungeon_event();

    // Rest of the player action code
}
